# Artefacts du Firebase CLI (générés localement, à ne pas versionner)
.firebase/
firebase-debug.log
firebase-debug.*.log
database-debug.log
.runtimeconfig.json

# Divers OS / éditeur
.DS_Store
Thumbs.db
